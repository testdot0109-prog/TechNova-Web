// Drop this in as your /about route's content.
// It only renders the content card(s) — your existing Sidebar/Topbar
// and the gray page background stay exactly as they are.
//
// Example (React Router, matching your screenshot's layout):
//   import AboutTechNova from "./AboutTechNova";
//   <Route path="/about" element={<AboutTechNova />} />
// If your layout wraps routed pages in <main className="p-8 bg-slate-50 ...">,
// this component just needs to sit inside that <main> — no extra wrapper needed.

import React from "react";

const STAGES = [
  {
    icon: "🎯",
    title: "Competency Assessment",
    body: "Reads an official's profile against Statistical, Technical, Digital Governance and Behavioural frameworks.",
  },
  {
    icon: "📊",
    title: "Skill-Gap Analysis",
    body: "Compares the assessed profile to what the role actually requires, and surfaces the gaps that matter.",
  },
  {
    icon: "🎓",
    title: "Personalised Recommendations",
    body: "Matches gaps to courses from iGOT Karmayogi and NSSTA's TPAC programmes, ranked by relevance.",
  },
  {
    icon: "✅",
    title: "AI-Powered Assessment",
    body: "Generates quizzes from uploaded material, scores them instantly, and updates the competency score.",
  },
];

const DOMAINS = ["Statistical", "Technical", "Digital Governance", "Behavioural & Managerial"];

const PLATFORMS = [
  {
    name: "iGOT Karmayogi",
    detail: "Course catalogue, enrolment tracking, competency-score sync",
  },
  {
    name: "NSSTA TPAC",
    detail: "Recommended training programmes feed into personalised picks",
  },
];

export default function AboutTechNova() {
  return (
    <div className="max-w-3xl space-y-6">
      {/* Hero card */}
      <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
        <h1 className="text-3xl font-bold text-slate-900">About TechNova</h1>
        <p className="text-slate-600 mt-2">
          Smart Learning. Smarter Skills. Future-Ready Workforce.
        </p>

        <p className="text-slate-700 leading-relaxed mt-6">
          TechNova exists to help officials in India's statistical system see
          their own competency gaps clearly, and get pointed toward training
          that actually closes them — instead of generic courses that may or
          may not fit their role.
        </p>

        <div className="flex flex-wrap gap-2 mt-5">
          {DOMAINS.map((d) => (
            <span
              key={d}
              className="text-xs font-medium text-blue-700 bg-blue-50 border border-blue-100 rounded-full px-3 py-1"
            >
              {d}
            </span>
          ))}
        </div>
      </div>

      {/* How it works */}
      <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
        <h2 className="text-lg font-semibold text-slate-900">How it works</h2>
        <p className="text-slate-600 text-sm mt-1">
          One continuous loop — each stage feeds the next.
        </p>

        <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-5">
          {STAGES.map((s, i) => (
            <div
              key={s.title}
              className="border border-slate-200 rounded-lg p-4 hover:border-blue-200 hover:bg-blue-50/40 transition-colors"
            >
              <div className="flex items-center gap-2">
                <span className="text-lg">{s.icon}</span>
                <span className="text-xs font-semibold text-blue-600">
                  Step {i + 1}
                </span>
              </div>
              <h3 className="text-sm font-semibold text-slate-900 mt-2">
                {s.title}
              </h3>
              <p className="text-sm text-slate-600 mt-1 leading-relaxed">
                {s.body}
              </p>
            </div>
          ))}
        </div>
      </div>

      {/* Connected platforms */}
      <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
        <h2 className="text-lg font-semibold text-slate-900">Connected platforms</h2>
        <p className="text-slate-600 text-sm mt-1">
          TechNova doesn't work alone — it plugs into these.
        </p>

        <div className="space-y-3 mt-5">
          {PLATFORMS.map((p) => (
            <div
              key={p.name}
              className="flex items-center justify-between border border-slate-200 rounded-lg px-4 py-3"
            >
              <div>
                <p className="text-sm font-semibold text-slate-900">{p.name}</p>
                <p className="text-xs text-slate-500 mt-0.5">{p.detail}</p>
              </div>
              <span className="flex items-center gap-1.5 text-xs font-medium text-green-700 bg-green-50 border border-green-100 rounded-full px-2.5 py-1">
                <span className="w-1.5 h-1.5 rounded-full bg-green-500" />
                Connected
              </span>
            </div>
          ))}
        </div>
      </div>

      {/* Prototype notice */}
      <div className="bg-blue-50 border border-blue-100 rounded-xl p-5">
        <p className="text-blue-900 font-semibold text-sm">Prototype Notice</p>
        <p className="text-blue-800 text-sm leading-relaxed mt-1">
          This hackathon prototype uses demo data and simulated
          government-system integrations. Production deployment would require
          authorized APIs, validated competency frameworks, security controls,
          and government infrastructure.
        </p>
      </div>
    </div>
  );
}
